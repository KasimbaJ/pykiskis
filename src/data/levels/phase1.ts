import type { Level } from '../../types'

export const phase1Levels: Level[] = [
  {
    id: 1,
    phase: 1,
    levelMode: 'code',
    title: 'Hello World',
    concept: 'print()',
    description:
      "Every programming journey starts with \"Hello, World!\" The print() function is Python's way of displaying output to the screen. It takes text (called a string) inside quotation marks and shows it.",
    task: 'Use the print() function to display "Hello, World!" exactly as shown.',
    starterCode: '# Your first Python program!\n# Use print() to display a message\n',
    expectedOutput: 'Hello, World!',
    validationMode: 'exact',
    hints: [
      'You need to use the print() function.',
      'Put your text inside quotation marks within print().',
      'Type exactly: print("Hello, World!")',
    ],
    solution: 'print("Hello, World!")',
    explanation:
      'The print() function outputs text to the console. Strings (text) must be wrapped in quotes — either single (\') or double ("). print("Hello, World!") sends the text Hello, World! to the output.',
  },
  {
    id: 2,
    phase: 1,
    levelMode: 'code',
    title: 'Variables',
    concept: 'Variable assignment',
    description:
      'Variables are like labeled boxes that store data. In Python, you create a variable by giving it a name and assigning a value with the = sign. Variable names should be descriptive and use lowercase letters.',
    task: 'Create a variable called "name" with the value "Python", then print "My name is Python" using that variable.',
    starterCode: '# Create a variable and use it in a print statement\n',
    expectedOutput: 'My name is Python',
    validationMode: 'exact',
    hints: [
      'First assign a value to a variable, then use it in print().',
      'Use: name = "Python"',
      'Complete solution: name = "Python" then print("My name is " + name)',
    ],
    solution: 'name = "Python"\nprint("My name is " + name)',
    explanation:
      'Variables store values for later use. The = operator assigns the value on the right to the name on the left. You can concatenate (join) strings using the + operator.',
  },
  {
    id: 3,
    phase: 1,
    levelMode: 'code',
    title: 'String Manipulation',
    concept: 'String methods: .upper() .lower() .replace()',
    description:
      "Strings in Python come with built-in methods. .upper() converts all characters to uppercase, .lower() to lowercase, and .replace() swaps part of a string for something else.",
    task: 'Create a variable word = "hello", then print it in uppercase on one line and lowercase on the next.',
    starterCode: '# Use string methods .upper() and .lower()\nword = "hello"\n',
    expectedOutput: 'HELLO\nhello',
    validationMode: 'exact',
    hints: [
      'String methods are called with a dot: word.upper()',
      'You need two print statements — one for upper, one for lower.',
      'print(word.upper()) then print(word.lower())',
    ],
    solution: 'word = "hello"\nprint(word.upper())\nprint(word.lower())',
    explanation:
      ".upper() returns a new string with all letters capitalized. .lower() returns one with all letters in lowercase. These methods don't change the original string — they return a new one.",
  },
  {
    id: 4,
    phase: 1,
    levelMode: 'code',
    title: 'Input and Print',
    concept: 'input() function',
    description:
      'The input() function pauses your program and waits for the user to type something. It always returns a string. When you click Run, the program will pause and show you an input box — type "Alice" and press Enter to see the result.',
    task: 'Ask for a name using input() and print "Hello, Alice!" — when prompted, type "Alice" and press Enter.',
    starterCode: '# input() is simulated — it will return "Alice"\n# Ask for a name and greet them\n',
    expectedOutput: 'Hello, Alice!',
    validationMode: 'exact',
    inputValues: ['Alice'],
    hints: [
      'Use input() to get a name, then use it in a print statement.',
      'name = input("Enter your name: ")',
      'name = input("Enter your name: ") then print("Hello, " + name + "!")',
    ],
    solution: 'name = input("Enter your name: ")\nprint("Hello, " + name + "!")',
    explanation:
      "input() reads user input as a string. You can store it in a variable and use string concatenation (+) to build a greeting.",
  },
  {
    id: 5,
    phase: 1,
    levelMode: 'code',
    title: 'Variable Naming Rules',
    concept: 'snake_case, naming conventions',
    description:
      'Python variables must follow naming rules: start with a letter or underscore, use only letters/numbers/underscores, no spaces, and are case-sensitive. The convention is snake_case for variable names.',
    task: 'Create variables: first_name = "John", age = 25, then print "John is 25 years old".',
    starterCode: '# Use proper snake_case variable names\n',
    expectedOutput: 'John is 25 years old',
    validationMode: 'exact',
    hints: [
      'Use underscores between words: first_name not firstName.',
      'first_name = "John" and age = 25',
      'print(first_name + " is " + str(age) + " years old")',
    ],
    solution: 'first_name = "John"\nage = 25\nprint(first_name + " is " + str(age) + " years old")',
    explanation:
      'snake_case means lowercase words separated by underscores. Notice how str(age) converts the integer 25 to a string so it can be concatenated. You cannot use + to join a string and an integer directly.',
  },
  {
    id: 6,
    phase: 1,
    levelMode: 'code',
    title: 'Math Operations',
    concept: 'Operators: + - * / // % **',
    description:
      'Python supports all standard math operators: + (add), - (subtract), * (multiply), / (divide — always float), // (integer division), % (modulo/remainder), and ** (exponent).',
    task: 'Given a = 10 and b = 3, print a+b, a-b, a*b, a//b, and a%b each on a new line.',
    starterCode: '# Arithmetic operators\na = 10\nb = 3\n',
    expectedOutput: '13\n7\n30\n3\n1',
    validationMode: 'exact',
    hints: [
      'Use a separate print() for each operation.',
      'print(a+b), print(a-b), print(a*b)...',
      'print(a+b)\nprint(a-b)\nprint(a*b)\nprint(a//b)\nprint(a%b)',
    ],
    solution: 'a = 10\nb = 3\nprint(a + b)\nprint(a - b)\nprint(a * b)\nprint(a // b)\nprint(a % b)',
    explanation:
      '10+3=13, 10-3=7, 10*3=30. // is integer division: 10//3=3 (drops the decimal). % is modulo: 10%3=1 (the remainder after dividing).',
  },
  {
    id: 7,
    phase: 1,
    levelMode: 'code',
    title: 'Data Types',
    concept: 'int, float, str, bool',
    description:
      "Python has several built-in data types. int stores whole numbers, float stores decimals, str stores text, and bool stores True or False. Use type() to find out a value's type.",
    task: 'Print the type of each: 42, 3.14, "hello", and True — each on a new line.',
    starterCode: '# Explore Python data types with type()\n',
    expectedOutput: "<class 'int'>\n<class 'float'>\n<class 'str'>\n<class 'bool'>",
    validationMode: 'exact',
    hints: [
      'Use type() to get the type of a value: type(42)',
      'print(type(42)) will output <class \'int\'>',
      'print(type(42))\nprint(type(3.14))\nprint(type("hello"))\nprint(type(True))',
    ],
    solution: 'print(type(42))\nprint(type(3.14))\nprint(type("hello"))\nprint(type(True))',
    explanation:
      "type() returns the class of the value. int for whole numbers, float for decimals, str for text, bool for True/False. Python is dynamically typed — you don't declare types explicitly.",
  },
  {
    id: 8,
    phase: 1,
    levelMode: 'code',
    title: 'Type Conversion',
    concept: 'int(), float(), str()',
    description:
      'Sometimes you need to convert between types. int() converts to integer, float() to decimal, str() to string. This is called type casting or type conversion.',
    task: 'Convert the string "42" to an integer, add 8 to it, and print the result.',
    starterCode: '# Type conversion\nnumber_str = "42"\n',
    expectedOutput: '50',
    validationMode: 'exact',
    hints: [
      'Use int() to convert a string to an integer.',
      'number = int(number_str)',
      'number = int(number_str)\nprint(number + 8)',
    ],
    solution: 'number_str = "42"\nnumber = int(number_str)\nprint(number + 8)',
    explanation:
      'int("42") converts the string "42" to the integer 42. Then 42 + 8 = 50. Without int(), "42" + 8 would cause a TypeError since you cannot add a string and an integer.',
  },
  {
    id: 9,
    phase: 1,
    levelMode: 'code',
    title: 'Conditionals',
    concept: 'if / elif / else',
    description:
      "Conditional statements let your program make decisions. 'if' checks a condition — if True, the indented code runs. 'elif' checks another condition. 'else' runs when nothing else was True. Indentation is crucial!",
    task: "Given number = 7, check if it's positive, negative, or zero, and print the result.",
    starterCode: '# Check if a number is positive, negative, or zero\nnumber = 7\n',
    expectedOutput: 'Positive',
    validationMode: 'exact',
    hints: [
      'Use if/elif/else to check the three cases.',
      'if number > 0: print("Positive")',
      'if number > 0:\n    print("Positive")\nelif number < 0:\n    print("Negative")\nelse:\n    print("Zero")',
    ],
    solution: 'number = 7\nif number > 0:\n    print("Positive")\nelif number < 0:\n    print("Negative")\nelse:\n    print("Zero")',
    explanation:
      'The if statement evaluates number > 0. Since 7 > 0 is True, it prints "Positive" and skips the elif and else. Only the first True condition executes.',
  },
  {
    id: 10,
    phase: 1,
    levelMode: 'code',
    title: 'Logical Operators',
    concept: 'and / or / not',
    description:
      'Logical operators combine Boolean expressions. "and" returns True only if both sides are True. "or" returns True if at least one side is True. "not" flips True to False and vice versa.',
    task: 'Given a = True and b = False, print the results of: a and b, a or b, not b — each on a new line.',
    starterCode: '# Explore logical operators\na = True\nb = False\n',
    expectedOutput: 'False\nTrue\nTrue',
    validationMode: 'exact',
    hints: [
      'Use print() with each logical expression.',
      'print(a and b) — both must be True for "and" to be True.',
      'print(a and b)\nprint(a or b)\nprint(not b)',
    ],
    solution: 'a = True\nb = False\nprint(a and b)\nprint(a or b)\nprint(not b)',
    explanation:
      'True and False = False (both must be True). True or False = True (at least one is True). not False = True (flips the value).',
  },
  {
    id: 11,
    phase: 1,
    levelMode: 'code',
    title: 'Random Numbers',
    concept: 'random module, seed()',
    description:
      'The random module generates pseudo-random numbers. random.randint(a, b) returns a random integer between a and b (inclusive). Using random.seed() makes results reproducible — same seed gives same sequence.',
    task: 'Import random, set seed(42), then print randint(1, 10).',
    starterCode: '# Import and use the random module\n',
    expectedOutput: '2',
    validationMode: 'exact',
    hints: [
      'import random at the top, then use random.seed() and random.randint()',
      'random.seed(42) makes results deterministic.',
      'import random\nrandom.seed(42)\nprint(random.randint(1, 10))',
    ],
    solution: 'import random\nrandom.seed(42)\nprint(random.randint(1, 10))',
    explanation:
      'random.seed(42) initialises the random number generator with value 42, making it produce the same sequence every time. random.randint(1, 10) picks a number from 1 to 10 inclusive.',
  },
  {
    id: 12,
    phase: 1,
    levelMode: 'code',
    title: 'Error Handling Basics',
    concept: 'try / except',
    description:
      'Errors (exceptions) crash your program unless you handle them. The try block contains code that might fail. If an error occurs, Python jumps to the except block instead of crashing.',
    task: 'Wrap int("hello") in a try/except block and print "Caught an error!" if it fails.',
    starterCode: '# Handle an error gracefully\n',
    expectedOutput: 'Caught an error!',
    validationMode: 'exact',
    hints: [
      'Use a try: block for the risky code and except: for the handler.',
      'try:\n    int("hello")\nexcept:',
      'try:\n    int("hello")\nexcept:\n    print("Caught an error!")',
    ],
    solution: 'try:\n    int("hello")\nexcept:\n    print("Caught an error!")',
    explanation:
      'int("hello") raises a ValueError because "hello" cannot be converted to an integer. The except block catches the error and prints the message instead of crashing.',
  },
  {
    id: 13,
    phase: 1,
    levelMode: 'code',
    title: 'Defining Functions',
    concept: 'def, parameters, return',
    description:
      "Functions are reusable blocks of code defined with the 'def' keyword. They can take parameters (inputs) and return values. Functions help organise code and avoid repetition.",
    task: 'Define a function add(a, b) that returns the sum of a and b, then print add(3, 5).',
    starterCode: '# Define a function and call it\n',
    expectedOutput: '8',
    validationMode: 'exact',
    hints: [
      'Use def to define the function, and return to send back the result.',
      'def add(a, b): return a + b',
      'def add(a, b):\n    return a + b\nprint(add(3, 5))',
    ],
    solution: 'def add(a, b):\n    return a + b\n\nprint(add(3, 5))',
    explanation:
      'def add(a, b) defines a function named "add" with two parameters. return a + b sends the sum back to the caller. print(add(3, 5)) calls the function with 3 and 5, gets back 8, and prints it.',
  },
  {
    id: 14,
    phase: 1,
    levelMode: 'code',
    title: 'For Loops',
    concept: 'for, range()',
    description:
      'For loops repeat code for each item in a sequence. range(start, stop) generates numbers from start up to (but not including) stop. range(1, 6) gives 1, 2, 3, 4, 5.',
    task: 'Use a for loop with range() to print numbers 1 through 5, each on a new line.',
    starterCode: '# Use a for loop to print numbers\n',
    expectedOutput: '1\n2\n3\n4\n5',
    validationMode: 'exact',
    hints: [
      'Use for and range() together.',
      'range(1, 6) generates numbers 1 through 5.',
      'for i in range(1, 6):\n    print(i)',
    ],
    solution: 'for i in range(1, 6):\n    print(i)',
    explanation:
      'range(1, 6) produces the sequence 1, 2, 3, 4, 5. The for loop assigns each number to i and executes the indented print(i). The stop value (6) is excluded.',
  },
  {
    id: 15,
    phase: 1,
    levelMode: 'code',
    title: 'Code Blocks & Indentation',
    concept: 'Indentation rules, code blocks',
    description:
      'Python uses indentation (4 spaces) to define code blocks — unlike other languages that use braces {}. Getting indentation wrong is one of the most common beginner mistakes.',
    task: 'Fix the code: check if 4 is even (4 % 2 == 0), print "Even", then print "Done" outside the if.',
    starterCode: '# Fix the indentation\nnumber = 4\nif number % 2 == 0:\nprint("Even")\nprint("Done")\n',
    expectedOutput: 'Even\nDone',
    validationMode: 'exact',
    hints: [
      'The print("Even") line needs to be indented inside the if block.',
      'print("Done") should NOT be indented — it runs regardless.',
      'if number % 2 == 0:\n    print("Even")\nprint("Done")',
    ],
    solution: 'number = 4\nif number % 2 == 0:\n    print("Even")\nprint("Done")',
    explanation:
      'In Python, the if block body must be indented by 4 spaces. print("Done") is outside the if block so it always runs. Incorrect indentation causes IndentationError.',
  },
  {
    id: 16,
    phase: 1,
    levelMode: 'code',
    title: 'While Loops',
    concept: 'while, counter',
    description:
      'While loops repeat code as long as a condition is True. You typically use a counter variable that changes each iteration to eventually make the condition False and stop the loop.',
    task: 'Use a while loop to count down from 5 to 1, printing each number on a new line.',
    starterCode: '# Use a while loop to count down\n',
    expectedOutput: '5\n4\n3\n2\n1',
    validationMode: 'exact',
    hints: [
      'Start with count = 5 and loop while count > 0.',
      'Print the count, then decrease it by 1 each iteration.',
      'count = 5\nwhile count > 0:\n    print(count)\n    count -= 1',
    ],
    solution: 'count = 5\nwhile count > 0:\n    print(count)\n    count -= 1',
    explanation:
      'The loop starts with count = 5. Each iteration prints count and decreases it by 1. count -= 1 is shorthand for count = count - 1. When count reaches 0, the condition becomes False and the loop stops.',
  },
  {
    id: 17,
    phase: 1,
    levelMode: 'code',
    title: 'Flowchart to Code',
    concept: 'Translating logic to code',
    description:
      'Flowcharts visually represent logic. A diamond shape is a decision point (if/else). Translating flowcharts to code is a key skill — it helps you think algorithmically before writing.',
    task: 'Translate this logic: if age >= 18, print "You can vote", otherwise print "Too young". Use age = 20.',
    starterCode: '# Translate the flowchart logic\nage = 20\n',
    expectedOutput: 'You can vote',
    validationMode: 'exact',
    hints: [
      'This is a simple if/else — two branches.',
      'if age >= 18: print("You can vote")',
      'if age >= 18:\n    print("You can vote")\nelse:\n    print("Too young")',
    ],
    solution: 'age = 20\nif age >= 18:\n    print("You can vote")\nelse:\n    print("Too young")',
    explanation:
      '20 >= 18 is True so "You can vote" prints. The >= operator means "greater than or equal to". Flowcharts map directly to if/elif/else chains in Python.',
  },
  {
    id: 18,
    phase: 1,
    levelMode: 'code',
    title: 'Positional & Keyword Args',
    concept: 'Default parameters, keyword arguments',
    description:
      'Functions can have default parameter values — used when the caller does not provide that argument. Keyword arguments let you pass arguments by name, in any order.',
    task: 'Define greet(name, greeting="Hello"), call it as greet("Alice", greeting="Hi") and print the result.',
    starterCode: '# Default and keyword arguments\n',
    expectedOutput: 'Hi, Alice!',
    validationMode: 'exact',
    hints: [
      'Define the function with a default value for greeting.',
      'def greet(name, greeting="Hello"):',
      'def greet(name, greeting="Hello"):\n    print(f"{greeting}, {name}!")\ngreet("Alice", greeting="Hi")',
    ],
    solution: 'def greet(name, greeting="Hello"):\n    print(f"{greeting}, {name}!")\n\ngreet("Alice", greeting="Hi")',
    explanation:
      'greeting="Hello" is the default value — used if not provided. When calling greet("Alice", greeting="Hi"), we override the default. f-strings (f"...") embed variables directly.',
  },
  {
    id: 19,
    phase: 1,
    levelMode: 'code',
    title: 'Python Lists',
    concept: 'list, append, len, indexing',
    description:
      'Lists are ordered collections that can hold multiple items of any type. Create them with square brackets []. Use .append() to add items to the end, and len() to count items.',
    task: 'Create a list fruits = ["apple", "banana", "cherry"], append "date", then print its length.',
    starterCode: '# Work with lists\n',
    expectedOutput: '4',
    validationMode: 'exact',
    hints: [
      'Create the list, use .append() to add an item, then use len().',
      'fruits = ["apple", "banana", "cherry"] then fruits.append("date")',
      'fruits = ["apple", "banana", "cherry"]\nfruits.append("date")\nprint(len(fruits))',
    ],
    solution: 'fruits = ["apple", "banana", "cherry"]\nfruits.append("date")\nprint(len(fruits))',
    explanation:
      'Lists start with 3 items. .append("date") adds a 4th item to the end. len() returns the number of items — now 4. Lists are mutable (changeable) unlike tuples.',
  },
  {
    id: 20,
    phase: 1,
    levelMode: 'code',
    title: 'Python Dictionaries',
    concept: 'dict, keys, values',
    description:
      'Dictionaries store key-value pairs using curly braces {}. Each key maps to a value. Access values with dict["key"]. Keys must be unique and are usually strings.',
    task: 'Create student = {"name": "Alice", "grade": "A"}, then print "Alice got an A".',
    starterCode: '# Work with dictionaries\n',
    expectedOutput: 'Alice got an A',
    validationMode: 'exact',
    hints: [
      'Access dictionary values with student["name"] and student["grade"].',
      'Use string concatenation or f-strings to build the output.',
      'student = {"name": "Alice", "grade": "A"}\nprint(student["name"] + " got an " + student["grade"])',
    ],
    solution: 'student = {"name": "Alice", "grade": "A"}\nprint(student["name"] + " got an " + student["grade"])',
    explanation:
      'student["name"] returns "Alice" and student["grade"] returns "A". String concatenation (+) joins the pieces. Dictionaries are perfect for structured data with named fields.',
  },
  {
    id: 21,
    phase: 1,
    levelMode: 'code',
    title: 'Nested Collections',
    concept: 'list of dicts, nested access',
    description:
      'Collections can be nested — lists can contain dictionaries, and dictionaries can contain lists. Access nested data by chaining indexes and keys.',
    task: 'Given students = [{"name": "Alice"}, {"name": "Bob"}], print the name of the second student.',
    starterCode: '# Access nested data\nstudents = [{"name": "Alice"}, {"name": "Bob"}]\n',
    expectedOutput: 'Bob',
    validationMode: 'exact',
    hints: [
      'First access index 1 (second item), then access the "name" key.',
      'students[1] gives the second dict.',
      'print(students[1]["name"])',
    ],
    solution: 'students = [{"name": "Alice"}, {"name": "Bob"}]\nprint(students[1]["name"])',
    explanation:
      'students[1] accesses index 1 (second element, since Python is 0-indexed). Then ["name"] accesses the name key of that dictionary. Chain [] operators to navigate nested structures.',
  },
  {
    id: 22,
    phase: 1,
    levelMode: 'code',
    title: 'Return vs Print',
    concept: 'return value semantics',
    description:
      'A crucial distinction: print() shows a value on screen but the function returns None. return sends a value back to the caller which can be stored or used. Most functions should return, not print.',
    task: 'Write a function double(n) that returns n*2. Call it, store the result in a variable, then print it.',
    starterCode: '# Return vs print distinction\n',
    expectedOutput: '42',
    validationMode: 'exact',
    hints: [
      'Use return inside the function, not print.',
      'def double(n): return n * 2',
      'def double(n):\n    return n * 2\nresult = double(21)\nprint(result)',
    ],
    solution: 'def double(n):\n    return n * 2\n\nresult = double(21)\nprint(result)',
    explanation:
      'double(21) returns 42 which is stored in result. Then print(result) shows it. If we used print(n*2) inside the function, we could not store the value — it would just display and return None.',
  },
  {
    id: 23,
    phase: 1,
    levelMode: 'code',
    title: 'Docstrings & Comments',
    concept: 'Triple-quote docstrings, # comments',
    description:
      'Comments (#) explain code for humans and are ignored by Python. Docstrings are special strings inside functions/classes that document them — accessible via __doc__.',
    task: 'Write a function add(a, b) with a docstring "Adds two numbers.", then print its __doc__.',
    starterCode: '# Add a docstring to a function\n',
    expectedOutput: 'Adds two numbers.',
    validationMode: 'contains',
    hints: [
      'A docstring goes right after the def line, in triple quotes.',
      'def add(a, b):\n    """Adds two numbers."""',
      'def add(a, b):\n    """Adds two numbers."""\n    return a + b\nprint(add.__doc__)',
    ],
    solution: 'def add(a, b):\n    """Adds two numbers."""\n    return a + b\n\nprint(add.__doc__)',
    explanation:
      'Triple quotes """ create a docstring — a string stored as the function\'s __doc__ attribute. print(add.__doc__) retrieves and displays it. Good docstrings explain what a function does, its parameters, and return value.',
  },
  {
    id: 24,
    phase: 1,
    levelMode: 'code',
    title: 'Scope & Debugging',
    concept: 'Local/global scope, debugging techniques',
    description:
      'Scope determines where a variable can be accessed. Variables defined inside a function are local (only available there). Variables defined outside are global. Use the global keyword to modify global variables inside functions.',
    task: 'Create x = 10 globally. Define a function that reads x and prints it. Call the function, then print x again.',
    starterCode: '# Global vs local scope\nx = 10\n',
    expectedOutput: '10\n10',
    validationMode: 'exact',
    hints: [
      'Define a function that uses the global x without redefining it.',
      'def show_x():\n    print(x)',
      'def show_x():\n    print(x)\nshow_x()\nprint(x)',
    ],
    solution: 'x = 10\n\ndef show_x():\n    print(x)\n\nshow_x()\nprint(x)',
    explanation:
      'x = 10 is a global variable. show_x() can read it (read access is allowed). Both calls print 10. If you tried to reassign x inside the function without the global keyword, Python would create a new local x instead.',
  },
]
